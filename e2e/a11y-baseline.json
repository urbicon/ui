{
  "$comment": [
    "Allow-list for the axe passes in e2e/a11y.spec.ts.",
    "",
    "Every entry is a DEFERRAL, not an absolution: it names one axe rule, the",
    "pass and routes it applies to, and a node-level predicate that must match.",
    "A violation is only suppressed when EVERY one of its nodes is matched by",
    "some exception — a single unmatched node fails the test and prints the",
    "node. That is what makes this a ratchet: the known failures stay quiet,",
    "but any NEW node (a different colour pair, a different element) is red.",
    "",
    "An exception without a predicate (no `contrast`, no `htmlIncludes`) is",
    "rejected at load time, so nobody can accidentally mute a whole rule.",
    "",
    "Fields:",
    "  id            — stable handle, used in the 'unused exception' report",
    "  pass          — 'preview' (the [data-docs-preview] region) or",
    "                  'code' (the CodeExample CodePanel: toolbar + Shiki)",
    "  rule          — axe rule id",
    "  routes        — primitive slugs this applies to, or '*' for all",
    "  contrast      — for color-contrast: the exact fg/bg pair axe computed",
    "  htmlIncludes  — every string must appear in the node's outerHTML",
    "  reason        — why this is not fixed here",
    "  ref           — where the decision / follow-up lives",
    "",
    "When a row gets fixed, delete its entry — the suite reports exceptions",
    "that no longer match anything at the end of a full run."
  ],
  "exceptions": [
    {
      "id": "codepanel-punctuation-contrast",
      "pass": "code",
      "rule": "color-contrast",
      "routes": "*",
      "contrast": { "fg": "#9a968e", "bg": "#fbfaf6" },
      "reason": "Shiki `punctuation` / `meta.brace` / `punctuation.separator|terminator` / `meta.tag.start|end` render at #9a968e on the #fbfaf6 panel ground = 2.82:1 (AA needs 4.5:1). Deliberately deferred: comments already sit at the ramp's light limit, so darkening the very frequent punctuation is a coordinated ramp/aesthetic decision (light needs L<=0.173, dark L>=0.247), not a drive-by recolour. The fg colour is unique to those scopes in shiki-editorial-themes.ts, so this pair matches the punctuation tokens and nothing else.",
      "ref": "docs/technical-debt.md -> 'docs `CodePanel` Shiki punctuation token is below WCAG AA in both themes'"
    },

    {
      "id": "preview-text-on-primary-contrast",
      "pass": "preview",
      "rule": "color-contrast",
      "routes": [
        "accordion",
        "badge",
        "button",
        "card",
        "popover",
        "tab",
        "toast",
        "toolbar",
        "tooltip"
      ],
      "contrast": { "fg": "#f6f3ec", "bg": "#00845c" },
      "reason": "PRE-EXISTING, surfaced (not caused) by widening this harness. Systemic token pair: text-on-primary #f6f3ec on bg-primary #00845c = 4.25:1 at normal weight. Not fixable from e2e/ — it is a semantic-token decision in blocks/src/lib/style. Reported for triage.",
      "ref": "docs/technical-debt.md -> 'Semantic token pairs below WCAG AA'"
    },
    {
      "id": "preview-tab-segment-inactive-contrast",
      "pass": "preview",
      "rule": "color-contrast",
      "routes": ["segment-group", "tab"],
      "contrast": { "fg": "#6e6b64", "bg": "#e8e3e1" },
      "reason": "PRE-EXISTING. Inactive tab/segment label #6e6b64 on the interactive surface #e8e3e1 = 4.17:1. Semantic-token decision, outside e2e scope. Reported for triage.",
      "ref": "docs/technical-debt.md -> 'Semantic token pairs below WCAG AA'"
    },
    {
      "id": "preview-journey-timeline-quaternary-contrast",
      "pass": "preview",
      "rule": "color-contrast",
      "routes": ["journey-timeline"],
      "contrast": { "fg": "#b8b5ad", "bg": "#fbfaf6" },
      "reason": "PRE-EXISTING. text-text-quaternary #b8b5ad on #fbfaf6 = 1.96:1 — the same ramp stop the Shiki comment token was raised away from in 572b738. Semantic-token decision, outside e2e scope. Reported for triage.",
      "ref": "docs/technical-debt.md -> 'Semantic token pairs below WCAG AA'"
    },
    {
      "id": "preview-breadcrumb-current-contrast",
      "pass": "preview",
      "rule": "color-contrast",
      "routes": ["breadcrumb"],
      "contrast": { "fg": "#00845c", "bg": "#e2eee7" },
      "reason": "PRE-EXISTING. aria-current crumb: text-primary on bg-primary/10 = 3.95:1. Docs-demo composition over a token pair; outside e2e scope. Reported for triage.",
      "ref": "docs/technical-debt.md -> 'Docs demo markup carries four a11y defects the widened axe pass now pins'"
    },
    {
      "id": "preview-badge-white-on-warning-contrast",
      "pass": "preview",
      "rule": "color-contrast",
      "routes": ["badge"],
      "contrast": { "fg": "#ffffff", "bg": "#ef852e" },
      "reason": "PRE-EXISTING. Docs demo hardcodes `text-white` on `bg-warning` = 2.6:1 — a demo-markup bug in apps/docs (hardcoded colour, against the house rules), not a library defect. Lives in another session's tree. Reported for triage.",
      "ref": "docs/technical-debt.md -> 'Docs demo markup carries four a11y defects the widened axe pass now pins'"
    },
    {
      "id": "preview-segment-group-dark-skin-contrast",
      "pass": "preview",
      "rule": "color-contrast",
      "routes": ["segment-group"],
      "contrast": { "fg": "#17150f", "bg": "#062f26" },
      "reason": "PRE-EXISTING. Dark-skinned SegmentGroup demo: #17150f on #062f26 = 1.25:1 — near-invisible selected label. Demo markup in apps/docs, another session's tree. Reported for triage.",
      "ref": "docs/technical-debt.md -> 'Docs demo markup carries four a11y defects the widened axe pass now pins'"
    },
    {
      "id": "preview-segment-group-dark-skin-inactive-contrast",
      "pass": "preview",
      "rule": "color-contrast",
      "routes": ["segment-group"],
      "contrast": { "fg": "#6e6b64", "bg": "#070c10" },
      "reason": "PRE-EXISTING. Same dark-skinned SegmentGroup demo, inactive label: #6e6b64 on #070c10 = 3.69:1. Demo markup in apps/docs, another session's tree. Reported for triage.",
      "ref": "docs/technical-debt.md -> 'Docs demo markup carries four a11y defects the widened axe pass now pins'"
    },
    {
      "id": "preview-combobox-unlabelled-demo-input",
      "pass": "preview",
      "rule": "label",
      "routes": ["combobox"],
      "htmlIncludes": ["role=\"combobox\""],
      "reason": "PRE-EXISTING. Combobox demos in apps/docs/.../combobox/Docs.svelte pass neither `label` nor `aria-label`, so the input has no accessible name (Combobox.svelte:902 correctly leaves naming to the consumer). Demo markup in another session's tree. Reported for triage.",
      "ref": "docs/technical-debt.md -> 'Docs demo markup carries four a11y defects the widened axe pass now pins'"
    }
  ]
}
