# Lookalikes

Each of these would be an invisible marker under a strict grammar; here each is an error.

<!-- Typecheck -->
```ts
export const a = 1;
```

<!-- typecheck stub drizzle-orm -->
```ts
export const b = 1;
```

<!-- typecheck --> and prose after it
```ts
export const c = 1;
```
